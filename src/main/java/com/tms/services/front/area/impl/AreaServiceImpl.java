package com.tms.services.front.area.impl;import com.tms.core.ServersManager;import com.tms.services.front.area.dao.AreaDao;import com.tms.services.front.area.AreaService;import com.tms.services.front.area.bean.Area;import org.springframework.stereotype.Service;import javax.annotation.Resource;@Service("areaServiceFront")public class AreaServiceImpl extends ServersManager<Area, AreaDao> implements		AreaService {    @Resource(name = "areaDaoFront")    @Override    public void setDao(AreaDao areaDao) {        this.dao = areaDao;    }}